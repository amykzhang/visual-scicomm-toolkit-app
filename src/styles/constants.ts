import color from "./color";

const constants = {
    comment: {
        totalWidth: 200,
        totalHeight: 50,
        background: color.darkerYellow,
        cornerRadius: 20,
        padding: 20,
    },
};

export default constants;
